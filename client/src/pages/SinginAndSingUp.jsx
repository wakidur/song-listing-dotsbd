import React, { Component } from 'react';
import { connect } from 'react-redux';

export const SinginAndSingUp = () => {
  return <div>Sing in ss</div>;
};

const mapStateToProps = (state) => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(SinginAndSingUp);
